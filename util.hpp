#include "eyo.hpp"
#include <vector>

extern bool find(const string s, const vector<Token> v);
extern bool split_token(vector<char> v, int i, const char *word, int index);
extern bool isChar(char data);
extern void index(const string word, const vector<char> vec);
extern bool isFunction(vector<char> v, int i);

